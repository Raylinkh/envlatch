interface:
  display_name: "EnvLatch"
  short_description: "Run commands with Keychain-backed API keys"
  default_prompt: "Use $envlatch to pair this named agent or host, inspect non-secret key and group metadata, then run the exact command with one or more selected Keychain-backed credentials without revealing or exporting secret values."
